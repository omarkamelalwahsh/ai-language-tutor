{
  "name": "career-copilot-workspace",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && venv/Scripts/python -m uvicorn app.main:app --reload",
    "dev": "npx concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "install:all": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
